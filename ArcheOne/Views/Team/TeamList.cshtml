@using ArcheOne.Database.Entities;
@model List<TeamMst>
@{
    Layout = "_BlankLayout";
}

<div class="row form-group">
    <div class="col-md-12">
        <table id="tblTeam" class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <td>
                        <b>Id</b>
                    </td>
                    <td>
                        <b>Team Lead</b>
                    </td>
                    <td>
                        <b>Team Member</b>
                    </td>
                    <td>
                        <b>Edit</b>
                    </td>
                    <td>
                        <b>Delete</b>
                    </td>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @item.Id
                        </td>
                        <td>
                            @item.TeamLeadId
                        </td>
                        <td>
                            @item.TeamMemberId
                        </td>
                        <td>
                            <button class="btn btn-info btn-edit"  Id="@item.Id" onlclick="AddEditUser(@item.Id)">Edit</button>
                        </td>
                        <td>
                            <button class="btn btn-danger btn-delete" Id="@item.Id" onlclick="DeleteUser(@item.Id)">Delete</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
</div>